# Optional. Defaults to "/api", which is proxied to the real host:
#   - dev:  Vite server.proxy (vite.config.ts)
#   - prod: netlify.toml redirect
# Set only if you want to call the API host directly (will hit CORS in browser).
VITE_API_URL=/api
